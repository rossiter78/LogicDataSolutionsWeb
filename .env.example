# Google Workspace appointment scheduling — public booking page URL for the /contact embed.
# Real value goes in .env.local (never committed).
NEXT_PUBLIC_BOOKING_URL=

# Cloudflare Web Analytics token (set in Cloudflare Pages env at deploy).
NEXT_PUBLIC_CF_ANALYTICS_TOKEN=
